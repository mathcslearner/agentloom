# Grafana datasource provisioning for the compose obs profile (ticket
# 7.1). Dashboards land in 7.5 under a provisioning/dashboards sibling.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
