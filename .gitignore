# Binaries and build output
bin/
dist/
*.exe
*.test
*.out

# Load-generator campaign output (cmd/loadgen --out default, ticket 19.2)
results/

# Go coverage
coverage.*
*.coverprofile

# Environment / secrets — never commit these (.env.example is the committed template)
.env
.env.*
!.env.example

# Editor / OS
.idea/
.vscode/
.DS_Store

# Frontend (web/, scaffolded in M17)
node_modules/
.next/
